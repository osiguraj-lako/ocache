# ocache
A high-level Olric client with native Protocol Buffer support for distributed caching in Go. Provides clean GetProto/PutProto APIs that handle binary serialization transparently, smart key generation from proto messages, and type-safe cache operations with boolean returns for cache misses.
